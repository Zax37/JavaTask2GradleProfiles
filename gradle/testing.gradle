ext {
    replaceVars = [
        "db.driverClass": "oracle.jdbc.OracleDriver",
        "db.connectionURL": "www.testing.com",
        "db.username": "testUser",
        "db.password": "testPassword",
    ]
}