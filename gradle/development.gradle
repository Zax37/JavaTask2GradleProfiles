ext {
    replaceVars = [
        "db.driverClass": "oracle.jdbc.OracleDriver",
        "db.connectionURL": "www.development.com",
        "db.username": "devUser",
        "db.password": "devPassword",
    ]
}